# VoxBill-AI-Voice-Enabled-Billing-System
VoxBill AI is a smart voice-enabled billing system that converts spoken input into structured invoices. It allows users to generate bills using voice commands, automatically calculates totals, and exports invoices as PDF using a modern React frontend and Spring Boot backend.
